specVersion: "v1"
serviceId: "ColdGend"
location: "europe-central2"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "coldgend-fdc"
      cloudSql:
        instanceId: "coldgend-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
